global:
  resolve_timeout: 5m

route:
  receiver: opspilot-default
  group_by:
    - alertname
    - severity
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: opspilot-default